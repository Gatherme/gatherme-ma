mutation registerUser($user: register!){
    register(user: $user){
        token
        error
        id
    }
}
query  existUsername($user: String!){
    userByUsername(username: $user){
        id
        username
    }
}