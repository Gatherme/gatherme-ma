query getUserByEmail($email: String!){
    userByEmail(email: $email){id email username}
}
mutation userSingIn($email:String!, $password: String!){
    singIn(account: {email: $email
        password: $password}){token message nickName email}
}
